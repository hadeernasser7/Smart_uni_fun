{
  "name": "smart-university-iot-bridge",
  "version": "1.0.0",
  "description": "Azure IoT Hub telemetry forwarding bridge to Node.js backend",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
